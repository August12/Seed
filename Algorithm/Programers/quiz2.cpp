#include <stdio.h>
#include <stdlib.h>
#include <algorithm>
using namespace std;

int solution(long len,long a[]) {
	long maxLen = 0;

}

int main() {
	long I = 5000;
	long V[] = {0,5,3};

	sort(V,V+3);

	printf("%d\n", solution(I,V));
}