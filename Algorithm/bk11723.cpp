#include <stdio.h>
#include <vector>
#include <string.h>

using namespace std;

int M;
vector <int> target;

int main(void) {
	scanf("%d",&M);
	for(int i=1; i<=M; i++) {
		char command[6];
		memset(command,"",sizeof(command));

		scanf("%s", command);
		
	}
}